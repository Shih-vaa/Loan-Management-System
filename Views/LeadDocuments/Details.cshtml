@if (User.IsInRole("admin") || User.IsInRole("office") || User.IsInRole("calling"))
{
    <a class="btn btn-sm btn-outline-primary mt-2" href="/LeadDocuments/Create?leadId=@Model.LeadId">
        📤 Upload Document
    </a>
    <a class="btn btn-outline-dark me-2" href="/LeadDocuments/Review?leadId=@Model.LeadId">
    📂 Review Documents
</a>

}
